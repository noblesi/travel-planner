<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noblesi.travelplanner.admin.trip.mapper.AdminRecommendRuleMapper">
	<!-- 추천 점수 규칙 조회 sql -->
	<select id="selectRecommendRule" resultType="com.noblesi.travelplanner.admin.trip.dto.AdminRecommendRuleDTO">
		select 
			rule_id as ruleId, 
			like_weight likeWeight, 
			view_weight viewWeight, 
			copy_weight copyWeight, 
			active_yn activeYn, 
			admin_id adminId, 
			created_at createdAt,
			updated_at updatedAt
		from
			(
			select
				rule_id, 
				like_weight, 
				view_weight, 
				copy_weight, 
				active_yn,
				admin_id, 
				created_at, 
				updated_at
			from
				recommend_rule
			where 
				active_yn = 'Y'
			order by
				updated_at desc, rule_id desc
			)
		where
			rownum = 1
	</select>
	<!-- 활성화 된 추천 점수 규칙 비활성화 -->
	<update id="updateRecommendRule" >
		update recommend_rule
		set
			active_yn = 'N',
			updated_at = systimestamp
		where
			active_yn = 'Y'
	</update>
	<!-- 새로운 규칙 추가 -->
	<insert id="insertRecommendRule">
	insert into recommend_rule (
		rule_id, 
		like_weight, 
		view_weight, 
		copy_weight, 
		active_yn,
		admin_id, 
		created_at, 
		updated_at
	) values (
		seq_recommend_rule.nextval,
		#{rule.likeWeight},	
		#{rule.viewWeight},	
		#{rule.copyWeight},	
		'Y'	,
		#{adminId},
		systimestamp,
		systimestamp	
	)	
	</insert>
</mapper>
