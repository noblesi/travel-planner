<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noblesi.travelplanner.mapper.PlanMemberMapper">

	<insert id="insertPlanMember">
		INSERT INTO PLAN_MEMBER (
			PLAN_ID,
			MEMBER_ID,
			PARTICIPANT_TYPE
		) VALUES (
			#{planId},
			#{memberId},
			#{participantType}
		)
	</insert>

</mapper>
