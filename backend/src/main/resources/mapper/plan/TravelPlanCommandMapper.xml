<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noblesi.travelplanner.mapper.TravelPlanCommandMapper">

	<select id="nextTravelPlanId" resultType="long">
		SELECT SEQ_TRAVEL_PLAN.NEXTVAL
		  FROM DUAL
	</select>

	<insert id="insertTravelPlan" parameterType="com.noblesi.travelplanner.domain.plan.TravelPlan">
		INSERT INTO TRAVEL_PLAN (
			PLAN_ID,
			OWNER_MEMBER_ID,
			TITLE,
			REGION_CODE,
			START_DATE,
			END_DATE,
			VISIBILITY
		) VALUES (
			#{planId},
			#{ownerMemberId},
			#{title},
			#{regionCode},
			#{startDate},
			#{endDate},
			#{visibility}
		)
	</insert>

	<update id="updateTravelDates">
		UPDATE TRAVEL_PLAN
		   SET START_DATE = #{startDate},
		       END_DATE = #{endDate},
		       VERSION_NO = VERSION_NO + 1,
		       UPDATED_AT = CURRENT_TIMESTAMP
		 WHERE PLAN_ID = #{planId}
		   AND OWNER_MEMBER_ID = #{memberId}
		   AND PLAN_STATUS = 'ACTIVE'
		   AND VERSION_NO = #{versionNo}
	</update>

	<update id="updateTravelPlanMetadata">
		UPDATE TRAVEL_PLAN
		   SET TITLE = #{title},
		       VISIBILITY = #{visibility},
		       VERSION_NO = VERSION_NO + 1,
		       UPDATED_AT = CURRENT_TIMESTAMP
		 WHERE PLAN_ID = #{planId}
		   AND OWNER_MEMBER_ID = #{memberId}
		   AND PLAN_STATUS = 'ACTIVE'
		   AND VERSION_NO = #{versionNo}
	</update>

</mapper>
