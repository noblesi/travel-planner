<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.noblesi.travelplanner.mapper.MemberMapper">

	<resultMap id="loginUser" type="com.noblesi.travelplanner.domain.login.LoginUser">
		<constructor>
			<idArg column="MEMBER_ID" javaType="_long" />
			<arg column="EMAIL" javaType="string" />
			<arg column="NAME" javaType="string" />
			<arg column="NICKNAME" javaType="string" />
			<arg column="PROFILE_IMAGE_URL" javaType="string" />
			<arg column="GENDER_CODE" javaType="string" />
			<arg column="PHONE_NUMBER" javaType="string" />
			<arg column="BIRTH_DATE" javaType="date" />
			<arg column="MEMBER_STATUS" javaType="string" />
			<arg column="CREATED_AT" javaType="timestamp" />
			<arg column="UPDATED_AT" javaType="timestamp" />
			<arg column="WITHDRAWN_AT" javaType="timestamp" />
		</constructor>
	</resultMap>

	<select id="selectLogin" resultMap="loginUser">
        select 
        memeber_id as id, 
        email, 
        name,
        nickname,
        profile_image_url as profileImageUrl,
        gender_code as gender, 
        phone_number as phone
        where 
        email = #{email} and password_hash = #{password}
    </select>
</mapper>
